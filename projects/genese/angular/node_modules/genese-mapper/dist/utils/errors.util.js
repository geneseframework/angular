import * as chalk from 'chalk';
export function throwCustom(message, value) {
    if (message === void 0) { message = 'Error encountered'; }
    if (value === void 0) { value = ''; }
    console.log(chalk.redBright(message), value);
    throw Error(message);
}
